<div class="col mb-4 border border-dark post">
  <div class="d-flex justify-content-between post-header">
    <h2>
      <a href="/post/{{id}}">{{title}}</a>
    </h2>
    <h3>
        Posted by {{user.username}} on {{format_date date_created}}
    </h3>
  </div>
  <div class="col-md-12 post-body">
    <p>
      {{content}}
    </p>
  </div>
</div>